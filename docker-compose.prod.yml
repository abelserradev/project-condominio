# Docker Compose para producción (Coolify)
# Coolify asigna dominios a cada servicio; las variables se configuran en la UI
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: condominio-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      MONGODB_URI: ${MONGODB_URI}
      FRONTEND_URL: ${FRONTEND_URL}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_USUARIO: ${ADMIN_USUARIO}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      SEED_ADMIN: ${SEED_ADMIN:-true}
    depends_on:
      - mongodb
    networks:
      - condominio_network

  mongodb:
    image: mongo:7
    container_name: condominio-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: condominio
    volumes:
      - mongodb_data:/data/db
    networks:
      - condominio_network

networks:
  condominio_network:
    driver: bridge

volumes:
  mongodb_data:
